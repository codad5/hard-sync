[package]
name = "hsync"
version = "0.1.0"
edition = "2024"
description = "Sync files between two paths. Auto-detects drives by UUID/label."

[[bin]]
name = "hsync"
path = "src/main.rs"

[dependencies]
chrono = "0.4.44"
colored = "3.1.1"
fli = "1.2.2"
hard-sync-core = { path = "../core" }
