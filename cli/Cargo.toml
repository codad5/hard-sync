[package]
name = "hsync"
version = "0.1.0"
edition = "2024"
description = "Sync files between two paths. Auto-detects drives by UUID/label."

[[bin]]
name = "hsync"
path = "src/main.rs"

[dependencies]
colored = "3.1.1"
fli = "1.2.0"
hard-sync-core = { path = "../core" }
